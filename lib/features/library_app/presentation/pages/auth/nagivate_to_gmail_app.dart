import 'package:flutter/material.dart';

class NagivateToGmailApp extends StatelessWidget {
  const NagivateToGmailApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Navigate to Gmail App Page')));
  }
}
